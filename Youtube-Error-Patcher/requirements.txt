# WGT YouTube Patcher Requirements
# GUI uses tkinter which is included with Python

# Optional: For better HTTP handling (not required, uses urllib by default)
# requests>=2.28.0

# No external dependencies required - uses Python standard library only
# - tkinter (GUI)
# - urllib (HTTP requests)
# - zipfile (WGT extraction/repacking)
# - json (GitHub API parsing)
# - re (regex for patching)
# - threading (async downloads)
